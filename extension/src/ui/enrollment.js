import { BehaviorTracker } from '../collector/tracker.js'; import { buildProfile } from '../engine/classifier.js';
export class EnrollmentFlow { constructor({input,next,step,progress,onComplete}){this.input=input;this.next=next;this.step=step;this.progress=progress;this.onComplete=onComplete;this.samples=[];this.tracker=null;this.pass=0;next.addEventListener('click',()=>this.capture());input.addEventListener('input',()=>next.disabled=input.value.trim().length<12)} begin(){this.pass=0;this.samples=[];this.beginPass()} beginPass(){this.pass++;this.input.value='';this.next.disabled=true;this.step.textContent=`Pass ${this.pass} of 3`;this.progress.style.width=`${this.pass/3*100}%`;this.tracker=new BehaviorTracker({inputs:[this.input],motionTarget:document,submitTarget:this.next}).start();setTimeout(()=>this.input.focus(),40)} capture(){const data=this.tracker.getFeatureVector();this.tracker.stop();if(data.sampleCount<3){this.next.disabled=true;this.next.textContent='Type a little more';setTimeout(()=>{this.next.textContent='Confirm pass →';this.next.disabled=false},900);return}this.samples.push(data);this.pass<3?this.beginPass():this.onComplete(buildProfile(this.samples));} }
